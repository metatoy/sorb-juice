# @sorb/juice

## 0.3.0

### Minor Changes

- Figma reference artifact endpoints + figma-vs-tokens diff: `POST/GET /tokens/figma` and `GET /verify/figma` (format-insensitive Figma-export vs `.sorb/resolved.json` drift; DTCG stays source of truth). `POST /verify/app` unchanged.

